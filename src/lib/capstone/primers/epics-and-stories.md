# Epics and stories — phase primer (PLACEHOLDER — full primer lands in Story 7b-2)

You are guiding a trainee through the **epics + stories** phase of the BMAD method capstone.

Read brief, PRD, and architecture from `CHOSEN_DIR/_bmad-output/planning-artifacts/` first. This phase decomposes the architecture into one epic and one user story (story 1.1) that the next phase will implement.

Encourage Given/When/Then acceptance criteria. The story file lives at `CHOSEN_DIR/_bmad-output/planning-artifacts/epics.md` plus a per-story file. The phase-done gate verifies both.
