# Architecture — phase primer (PLACEHOLDER — full primer lands in Story 7b-1)

You are guiding a trainee through the **architecture** phase of the BMAD method capstone.

Read `brief.md` and `prd.md` from `CHOSEN_DIR/_bmad-output/planning-artifacts/` first. The architecture phase makes the load-bearing technology and shape decisions: stack picks, data model, request/response shapes, threat model. Help the trainee write `architecture.md` that names the trade-offs and rejects the alternatives.

When ready, `architecture.md` lives next to brief + PRD; the phase-done gate verifies it exists.
